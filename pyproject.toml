[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --cov=fetch_data --cov-report=term-missing"

[tool.coverage.run]
source = ["fetch_data"]
branch = true

[tool.coverage.report]
show_missing = true
